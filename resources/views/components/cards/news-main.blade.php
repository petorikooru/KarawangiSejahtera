
<div class="max-w-full lg:max-w-[60%] mb-6 lg:mb-0 mx-10">
    <a href="{{ $link }}">

    <!-- Image Section: Full width on smaller screens, 60% width on larger screens -->
    <img src="{{ $image }}" alt="Main News" class="w-full rounded-lg mb-4">

    <!-- Title Section -->
    <h3 class="text-3xl lg:text-4xl font-semibold text-[#8B4513]">{{ $title }}</h3>

    <!-- Description Section -->
    <p class="text-gray-700 mt-4 lg:mt-5">{{ $description }}</p>

    </a>
</div>
